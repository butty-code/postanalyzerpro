# Post Analyzer Pro (Next.js App Router)

## Quick start
```bash
npm i
npm run dev
# open http://localhost:3000/api/ping  (should return JSON)
# then http://localhost:3000/dashboard
```

## Batch input format
Separate posts with a line containing only `---` (Windows/macOS safe).
